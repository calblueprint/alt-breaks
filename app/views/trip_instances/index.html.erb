<h1>Listing trip_instances</h1>

<table>
  <tr>
    <th>Year</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @trip_instances.each do |trip_instance| %>
  <tr>
    <td><%= trip_instance.year %></td>
    <td><%= trip_instance.description %></td>
    <td><%= link_to 'Show', trip_instance %></td>
    <td><%= link_to 'Edit', edit_trip_instance_path(trip_instance) %></td>
    <td><%= link_to 'Destroy', trip_instance, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Trip instance', new_trip_instance_path %>
