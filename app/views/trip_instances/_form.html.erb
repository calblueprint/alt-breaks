<%= form_for(@trip_instance) do |f| %>
  <% if @trip_instance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trip_instance.errors.count, "error") %> prohibited this trip_instance from being saved:</h2>

      <ul>
      <% @trip_instance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :year %><br />
    <%= f.number_field :year %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
