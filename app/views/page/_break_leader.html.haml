.member
  .avatar
    - if member.avatar.exists?
      = image_tag member.avatar.url(:small)
    - else
      = image_tag("/assets/cat.png")
  .name
    = member.name
  .title
    /need to fix this to pull from only this year.. should be sourcing break leaders only from this year
    = member.trip_permissions.first.trip_instance.trip.name
