<h1>Listing testimonies</h1>

<table>
  <tr>
    <th>Trip instance</th>
    <th>User</th>
    <th>Context</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @testimonies.each do |testimony| %>
  <tr>
    <td><%= testimony.trip_instance_id %></td>
    <td><%= testimony.user_id %></td>
    <td><%= testimony.context %></td>
    <td><%= link_to 'Show', trip_instance_testimony %></td>
    <td><%= link_to 'Edit', edit_trip_instance_testimony_path(testimony) %></td>
    <td><%= link_to 'Destroy', testimony, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Testimony', new_trip_instance_testimony_path %>
